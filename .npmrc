# 📦 Dependency Hoisting
# Effectively manage partial hoisting for the monorepo
shamefully-hoist=true

# 🔒 strictness & Safety
strict-peer-dependencies=false
auto-install-peers=true
legacy-peer-deps=false
engine-strict=true

# 📌 Versioning
# Save exact versions to package.json to avoid drift
save-exact=true

# 🧹 Cleanliness
# Allow creating lockfiles
lockfile=true

# 🚀 Performance & Network
prefer-offline=false
fetch-retries=3
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000

# 🌍 Publishing
# Ensure scoped packages are published publicly by default
access=public
